export enum TokenType {
    STANDARD = "STANDARD",
    LIQUIDITY = "LIQUIDITY"
}

export enum UnlockType {
    LINEAR = "LINEAR",
    FULL = "FULL"
}

export enum OwnerType {
    ME = "ME",
    OTHER = "OTHER"
}